server:
  port: 8082
spring:
  application:
    name: consumer-service
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    generate-ddl: true
  datasource:
    url: jdbc:mysql://localhost:3306/{테이블명}
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: {아이디}
    password: {비밀번호}
